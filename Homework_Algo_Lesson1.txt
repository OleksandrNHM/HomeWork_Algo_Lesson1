1. Посмотрите на схему, сопоставьте номер с его обозначением
	1) c - Номера (индексы) элементов массива
 	2) b - Название массива
 	3) a - Элементы массива

2. Выберите описание, которое характеризует Сортировку пузырьком (Bubble sorting): 
	b - Последовательно сравниваются значения соседних элементов и меняются местами 	элементы, если предыдущий больше последующего. Таким образом элементы с большим 	значением оказываются в конце списка, а с меньшим в начале.

3. Алгоритмизация – этo...
	a - процесс разработки алгоритма для решения какой-либо задачи.

4. Выберите то, что НЕ является характеристикой алгоритма: 
	d - Может быть двусмысленным

5. Какого алгоритма НЕ существует? 
	e - Наглый алгоритм

6. Посмотрите на картинку и выберите определение для каждого элемента блок-схемы: 
	1) b - начало (конец) алгоритма
	2) a - операционный блок
	3) c - цикл с параметром
	4) d - логический (условный) блок
	5) e - блок ввода/вывода
	

7. Вам дан алгоритм покупки продуктов в текстовом виде, расставьте операции, написанные на псевдокоде в правильном порядке.
	1) b - START
	2) a - READ buy products
	3) d - WHILE (all products bought) buy products
	4) c - END

START

